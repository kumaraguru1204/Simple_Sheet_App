<Window
    x:Class="Simple_Sheet_App.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Simple_Sheet_App"
    xmlns:canvas="using:Microsoft.Graphics.Canvas.UI.Xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Simple Sheet App">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel
            Orientation="Horizontal"
            Padding="10"
            Spacing="10"
            Grid.Row="0"
            Background="RosyBrown"
            >

            <TextBox x:Name="RowBox"
                     PlaceholderText="Enter Row"
                     Width="80"/>

            <TextBox x:Name="ColBox"
                     PlaceholderText="Enter Col"
                     Width="80"/>

            <TextBox x:Name="ValBox"
                     PlaceholderText="Enter Value"
                     Width="150"/>

            <Button Content="Set Value"
                    Click="InsertVal_Button"/>
            
            <Button Content="Get Value"
                    Click="Get_value"/>

            <Button Click="Undo_Click">
                <SymbolIcon Symbol="Undo"/>
            </Button>

            <Button Click="Redo_Click">
                <SymbolIcon Symbol="Redo"/>
            </Button>
            <DropDownButton Content="Actions">
                <DropDownButton.Flyout>
                    <Flyout Placement="Bottom">
                        <StackPanel Width="200"  Spacing="8" Padding="10">
                            <ComboBox x:Name="actionBox" PlaceholderText="Choose Action" Width="150" HorizontalAlignment="Center">
                                <x:String>Insert Row</x:String>
                                <x:String>Insert Column</x:String>
                                <x:String>Delete Row</x:String>
                                <x:String>Delete Column</x:String>
                            </ComboBox>
                            <TextBlock Text="Enter the row/col below" HorizontalAlignment="Center"/>
                            <TextBox x:Name="rowOrCol" Width="100" HorizontalAlignment="Center"/>
                            <Button Content="Make Action" Click="Make_Action" HorizontalAlignment="Center"/>

                        </StackPanel>
                    </Flyout>
                </DropDownButton.Flyout>
            </DropDownButton>


            <TextBlock x:Name="StatBox"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"/>
          
            
        </StackPanel>

        <ScrollViewer x:Name="SheetSV"
              Grid.Row="1"
              HorizontalScrollBarVisibility="Auto"
              VerticalScrollBarVisibility="Auto">

            <Grid>
                <canvas:CanvasControl x:Name="SheetCanvas"
                              Draw="SheetCanvas_Draw"
                              PointerPressed="SheetCanvas_PointerPressed"
                              DoubleTapped="SheetCanvas_DoubleTapped"/>

                <TextBox x:Name="EditBox"
                 Visibility="Collapsed"
                 FontSize="13"
                 BorderBrush="RosyBrown"
                 BorderThickness="2"
                 VerticalAlignment="Top"
                 HorizontalAlignment="Left"
                 KeyDown="EditBox_KeyDown"
                 LostFocus="EditBox_LostFocus"/>
            </Grid>

        </ScrollViewer>
    </Grid>

</Window>